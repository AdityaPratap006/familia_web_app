import React from 'react';
import { VideoCallArea } from './style';


const VideoCallOverlay: React.FC = () => {
    return (
        <VideoCallArea>

        </VideoCallArea>
    );
};

export default VideoCallOverlay;
